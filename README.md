Understand client-side event handling
Explain how user input events (like input or keyup) are captured in a web application.

Implement real-time character counting
Develop logic to dynamically count characters as the user types in an input field or textarea.

Apply DOM manipulation techniques
Update HTML elements in real time using JavaScript to display the character count.

Integrate HTML, CSS, and JavaScript
Build a small full stack front-end feature by combining structure, styling, and behavior.
